# Network tuning for online gaming
# Deploy to: /etc/sysctl.d/99-gaming-network.conf
# Enables TCP Fast Open (client+server) and disables slow start after idle
# to reduce latency for game connections.

net.ipv4.tcp_fastopen=3
net.ipv4.tcp_slow_start_after_idle=0
